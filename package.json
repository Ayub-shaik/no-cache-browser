{
  "name": "no-cache-browser",
  "version": "0.1.0",
  "private": true,
  "description": "Developer-focused browser with optional no-cache mode (MVP host)",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/host/main.js",
    "dev": "npm run build && node dist/host/main.js",
    "test": "npm run build && node --test dist/tests/smoke.test.js dist/tests/session-buffer.test.js dist/tests/linux-resolve.test.js dist/tests/export-headless.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/ws": "^8.5.13",
    "typescript": "^5.6.3"
  }
}
